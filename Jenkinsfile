pipeline {
    
    agent any  
 
    stages {
 
        stage('Build docker'){
            steps {
                sh 'docker ps -a'
                echo 'Init'
                echo '******************************'
            }
        }

    }
}
