pipeline {
    
    agent any  
 
    stages {
 
        stage('Build docker'){
            steps {
                sh 'node --v'
                echo 'Init'
                echo '******************************'
            }
        }

    }
}
